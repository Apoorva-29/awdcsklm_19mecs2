class Complete
{
    // Function for finding maximum and value pair
    public static int multiply (int arr[], int n)
    {
        int product=1,sum;
        for(int i=0;i<n;i+=2)
        {
                sum=arr[i]+arr[i+1];
                product=product*sum;
            }
        return(product);
    }
}
*****************************OUTPUT*****************************
For Input:
4
1 2 3 4
your output is: 
21
